---
title: Examples
description: Package-backed examples with focused previews and source code.
icon: FlaskConical
---

import { ExampleGallery } from '@/components/examples/example-gallery';

Examples are stored in `@eregister/wit-grid-examples` — the same package the demo app imports from, so what you
see here is exactly what runs there. Switch to the **Source** tab on any example to read the full file, syntax
highlighted at build time.

<ExampleGallery />
