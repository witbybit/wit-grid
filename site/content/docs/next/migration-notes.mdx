---
title: Migration Notes
description: Track breaking changes and release-specific upgrade notes.
---

Migration notes should be updated whenever a release changes public behavior, exported APIs, event payloads, or documented defaults.

## Current Line

`next` tracks unreleased documentation. When a release is cut, copy `next` into a fixed version directory and add release-specific migration notes there.
